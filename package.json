{
  "name": "my-telegram-media-downloader",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Chrome extension (Manifest V3) that adds a download button to Telegram Web's media viewer.",
  "scripts": {
    "build": "node build.js",
    "watch": "node build.js --watch",
    "package": "node package.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint ."
  },
  "devDependencies": {
    "@eslint/js": "^9.9.0",
    "esbuild": "^0.23.1",
    "eslint": "^9.9.0",
    "globals": "^15.9.0",
    "vitest": "^2.0.5"
  }
}
